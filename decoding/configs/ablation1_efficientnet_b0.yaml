experiment:
  name: ablation1_efficientnet_b0

model:
  architecture: efficientnet_b0
  pretrained: true

training:
  batch_size: 12
  num_epochs: 30
  learning_rate: 0.0001

data:
  image_size: 1024
  splits_path: data/splits.json
  metadata_path: "${PROJECT_DATA_ROOT}/watermark_encoding/data/metadata.json"
  images_path: "${PROJECT_DATA_ROOT}/watermark_encoding/data/images/"

output:
  checkpoint: "${PROJECT_DATA_ROOT}/decoding/checkpoints/ablation1_efficientnet_b0.pth"
  results: results/ablation1_efficientnet_b0.md

seed: 42
